# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
*.local

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?

# Local strategy/design docs — never publish
docs/superpowers/
.impeccable.md
.env.local

# Research artifacts — papers + experiments live in the repo working tree but
# are NEVER committed to the public repo. Priority is established via Zenodo DOIs;
# the public repo cites them, it does not contain the drafts or the measurements.
papers/
experiments/
.vercel
